# Elasticsearch Multimodal Pipeline

This project implements a multimodal data pipeline using:
- Text
- Images
- Audio
- Video

The pipeline is designed to index multimodal data into Elasticsearch
for search and retrieval use cases.

## File
- `multimodal.py` – Core multimodal ingestion and processing logic
